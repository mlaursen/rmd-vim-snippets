global !p
def upperfirst(t):
	if len(t) < 2:
		return '' if len(t) < 1 else t.capitalize()
	return t[0].capitalize() + t[1:]
def lowerdash(t):
	return '-'.join([ w.lower() for w in re.findall('[a-z]+|[A-Z]+[a-z]*', t) ])
endglobal

snippet useCH "useChoice" b
const [${1:value}, ${2:handleChange}] = useChoice(${3:""});$0
endsnippet

snippet useT "useToggle" b
const [${1:toggle}, ${2:enable}, ${3:disable}${4:, toggle}] = useToggle(${5:false});
endsnippet
